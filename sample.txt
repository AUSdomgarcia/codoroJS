GIT PRACTISE

generate ssh
ssh -keygen -t rsa -C "email address"

if new
enter passphrase: password
enter same passphrase again:

then ssh will be save on your competer
see the drive given.

sample cd /c/Users/Comp/Desktop/

display the list use command: 
* ls -la
* git clone (get the repo directory) "folder name"

Are you sure u want to continue connecting(y/n)  yes
enter passphrase

cd workspace 
then $ pwd command will see current directory

git status

touch sample.txt create text dynamically

vim sample.txt - #####################

git status =see status

git add sample.txt

git status

git commit -m" message for this edit "

if new
*** Please tell who are you ***
Run 
	git config --global user.email "you@email.com"
	git config --global user.name "username"

$ git commit -m" message "

git push origin master

git status 
git diff "filename"

git add


git status

git commit -m"message"


git push origin master

git checkout =revert


